# Mobile shell

This shell targets React Native 0.81 for Android and iOS with the New Architecture enabled. Generate native projects with the matching React Native Community template, then link `@smartvolume/audio-engine`. Native capture module contracts are in `packages/audio-engine/native`.
