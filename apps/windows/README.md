# Windows shell

Target React Native Windows 0.81 with Fabric and WinAppSDK. Generate the C++ application shell on a Windows runner and register the audio and endpoint-volume TurboModules from `packages/audio-engine/native/windows`.
