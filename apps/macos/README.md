# macOS shell

Target React Native macOS 0.81 with the New Architecture. Generate the AppKit shell with the matching template and register the CoreAudio capture and volume modules from `packages/audio-engine/native/macos`.
