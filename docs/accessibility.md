# Accessibility

Controls expose roles, selected states, labels, live progress, and alerts. Layouts support keyboard navigation, scalable text, wrapping navigation, high contrast, and reduced motion.

Release testing must include VoiceOver, TalkBack, Windows Narrator, macOS VoiceOver, keyboard-only use, 200 percent text scaling, RTL, color contrast, and permission/error announcements.
